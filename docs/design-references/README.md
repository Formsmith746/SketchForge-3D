# Design references

The original Tinkercad page was inspected live at desktop and mobile sizes. Its in-app screenshot capture timed out because the page renders four large videos, so the auditable reference set is the computed-style specifications in `docs/research/components/` plus the original media downloaded to `apps/web/public/assets/tinkercad/`.

Local desktop and mobile QA screenshots are written here during final validation.

